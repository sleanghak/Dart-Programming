/**
 * Syntax
 * 
 * (parameter_list){
 * statement(s)
 * }
 */

import 'dart:io';

void main() {
  var list = {"Vann", "Houy", "Mrr.Hak"};
  stdout.write("W3adda = Dart Anonymous Funtion!");

  list.forEach((item) {
    // stdout.write("${list.indexOf(item)} : $item");
  });
}
